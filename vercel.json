{
  "version": 2,
  "buildCommand": "node scripts/build-vercel.mjs",
  "installCommand": "npm ci",
  "outputDirectory": "dist/public",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
